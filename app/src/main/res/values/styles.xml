<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item> <!-- no title on Action Bar -->
        <item name="windowActionBar">false</item> <!-- no Action Bar -->
        <item name="android:windowFullscreen">true</item> <!-- no Status Bar -->
    </style>

    <style name="TransparentTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item> <!-- no title on Action Bar -->
        <item name="windowActionBar">false</item> <!-- no Action Bar -->
        <item name="android:windowFullscreen">true</item> <!-- no Status Bar -->
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

</resources>